import { U as UNSUPPORTED } from './_constants-DnfQ3JJx.js';

/* eslint-disable camelcase */
/* global hmUI */

var hmUI$1 = hmUI;
const {align} = hmUI;
// @ts-ignore
const {anim_status} = hmUI;
const {createDialog} = hmUI;
const {createWidget} = hmUI;
const {deleteWidget} = hmUI;
const {event} = hmUI;
const {getRtlLayout} = hmUI;
const {getScrollCurrentPage} = hmUI;
const {getTextLayout} = hmUI;
const {prop} = hmUI;
const {relayoutRtl} = hmUI;
const {scrollToPage} = hmUI;
const {setLayerScrolling} = hmUI;
const {setScrollView} = hmUI;
const {setStatusBarVisible} = hmUI;
const {showToast} = hmUI;
const {text_style} = hmUI;
const {updateStatusBarTitle} = hmUI;
const {widget} = hmUI;

const redraw = () => {};
const getImageInfo = imgPath => {
	throw new Error(UNSUPPORTED);
};

const setAppWidgetSize = option => {
	throw new Error(UNSUPPORTED);
};

const getAppWidgetSize = option => {
	throw new Error(UNSUPPORTED);
};

export { align, anim_status, createDialog, createWidget, hmUI$1 as default, deleteWidget, event, getAppWidgetSize, getImageInfo, getRtlLayout, getScrollCurrentPage, getTextLayout, prop, redraw, relayoutRtl, scrollToPage, setAppWidgetSize, setLayerScrolling, setScrollView, setStatusBarVisible, showToast, text_style, updateStatusBarTitle, widget };
